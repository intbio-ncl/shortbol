# references allows a chaining of Components.
# There is no limit to the length of chain that could be described.

TetR is a Protein()
LacI is a Protein()

tetRInverter is a FunctionalEntity()
(
  references(TetR,LacI)
  references(tetRInverter,LacI)
)