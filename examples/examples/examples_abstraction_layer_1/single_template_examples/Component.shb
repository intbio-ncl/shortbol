# Example Creates a Component of a ComponentDefinition and
# Adds this to another ComponentDefinition as a sub component.
pTetR is a Promoter()

pTetR_c is a Component()

tetRInverter is a DNAComponent()
(
  component = pTetR_c
)