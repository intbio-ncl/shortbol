@prefix sbol

ComponentInstance(type, def)
(
  Identified(type)
  definition = def
  access = sbol.public
)

PrivateComponentInstance(type, def)
(
  Identified(type)
  definition = def
  access = sbol.private
)

Component(def) 
(
  ComponentInstance(Component, def)
)

